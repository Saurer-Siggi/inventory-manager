version: '3.8'

services:
  app:
    image: ghcr.io/saurer-siggi/inventory-manager:latest
    container_name: saurer-siggi-inventory-manager
    restart: unless-stopped
    ports:
      - "3000:3000"
    env_file:
      - .env
